# Frontend environment variables. Copy to `.env` if needed.
# During development, Vite proxies /api to http://localhost:8000 automatically,
# so this can be left empty. Set it only if the backend is on another origin.
VITE_API_BASE_URL=
