import type { Achievement } from '@/types'

/**
 * Achievements data source.
 *
 * No achievements are invented. Add verified achievements below.
 * The Achievements section hides itself entirely while this list is empty.
 *
 * Example:
 * { id: 'a1', title: 'Achievement', description: 'Details.', date: '2025', source: 'Organizer' }
 */
export const achievements: Achievement[] = []
